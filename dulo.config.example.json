{
  "disabledTools": ["get_env"],
  "approval": {
    "mode": "ask",
    "tools": ["shell", "write_file", "edit_file", "http_request"],
    "gateMcpTools": true
  },
  "mcpServers": {
    "fs": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."]
    },
    "docs": {
      "url": "https://example.com/mcp",
      "headers": { "Authorization": "Bearer REPLACE_ME" },
      "enabled": false
    }
  }
}
